name: insect_detector
description: اپلیکیشن تشخیص حشرات قرنطینه‌ای ایران
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  tflite_flutter: ^0.10.4
  image_picker: ^1.0.7
  image: ^4.1.7
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/QU_Pests_39Epochs_YOLO26n_float32.tflite
    - assets/labels.txt
